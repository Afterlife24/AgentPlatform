__pycache__
.DS_Store
.env
.env.prod
.env.test
.env.local
*.env.local

# Per-server TLS material generated by setup_remote.sh / certbot. Never
# committed - each server has its own cert for its own domain.
certs/

# Conductor personal/per-machine overrides (settings.toml IS committed)
.conductor/settings.local.toml

# logs and run directory on production
/logs/
/run/
infrastructure/
prd/
.vercel

.devcontainer/install.local.sh
venv/
.venv/
.playwright-mcp
coturn/
*.wav
dograh_pcm_cache/
node_modules/

# Superpowers brainstorm mockups (local only)
.superpowers/
docs/superpowers/
.gstack/
